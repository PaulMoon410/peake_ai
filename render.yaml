services:
  - type: web
    name: peakebot-web
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 1 -k gthread -b 0.0.0.0:$PORT web_app:app
    autoDeploy: true
